{
  "name": "uifipill",
  "version": "1.0.0",
  "description": "UIFIPILL - WiFi suite desktop for Windows",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:nsis": "tauri build --bundler nsis",
    "lint": "node -c src/app.js",
    "vite": "vite",
    "vite:build": "vite build"
  },
  "dependencies": {
    "@tauri-apps/plugin-shell": "^2.3.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2",
    "vite": "^5.4.0"
  }
}
