{
  "$schema": "https://schema.tauri.app/config/capability/v1",
  "identifier": "default",
  "description": "Capabilities for UIFIPILL main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-open"
  ]
}
